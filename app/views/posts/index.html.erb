<%= render "home/header" %>

<div class="container">
	<div class="row">
		<div class="col-sm-3">
			<%= render "home/sidebar" %>
		</div>
		<div class="col-sm-8">
			<% @posts.each do |p| %>
				<%= p.title %>
			<% end %>
		</div>
	</div>
</div>